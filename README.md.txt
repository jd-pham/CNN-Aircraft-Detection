This was a college project, the main focus being the finetuning of a ResNet based CNN based on everything we had learned during the semester. Groups had to build their entire database (of images) from scratch; pre-existing databases were not allowed.

